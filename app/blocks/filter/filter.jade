mixin filter()
	+b.filter&attributes(attributes)
		+e.group
			+e('label') Город
			+e('input').city
		+e.group
			+e('label') Количество дней
			+e('input').days
		+e.group
			+e('label') Количество дней
			+e('input').date
		+e('button').submit Найти
